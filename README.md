Simple task manager project in CLI using JSON
Language: pt-BR
Saving data as a JSON at file: /home/abner/.local/share/trfs/trfs.json
Project URL: https://github.com/RyuUz-hub/Trfs-manager.git

        Total of 3 commands:

add - Add a new task with and ID higher than the last one
note: There's no way to add a task to a lower number currently (I don't wanna code that)

ls - list every task
ls -c - list every completed task

rm - removes a certain task
